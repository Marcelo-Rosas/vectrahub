# Homolog CT-e / MDF-e — OS-2026-08-0001

**Data:** 2026-08-01  
**Modo:** readiness (sem POST Focus)  
**Script:** `npx tsx scripts/smoke-cte-mdfe-homolog.ts`  
**Assert:** `docs/homolog/OS-2026-08-0001-cte-mdfe-readiness.json`  
**Resultado:** CT-e **NOT READY** · MDF-e **NOT READY**

## Infra OK

| Item | Status |
|------|--------|
| `emit-cte` / `emit-mdfe` Edge | ACTIVE no Hub |
| `FOCUS_NFE_AMBIENTE` + tokens | presentes |
| Shipper BOOST `emit_cte_via` | `cfn` |
| Shipper IE | ok (contribuinte + IE) |
| Seq homolog CT-e / MDF-e | `last_numero=0` (limpo) |
| Frota Hub | 43 drivers · 266 vehicles |

## Blockers CT-e (live)

1. ~~**Secrets endereço emitente**~~ — **SETADOS** 2026-08-01  
   (`VECTRA_LOGRADOURO`…`VECTRA_CRT=1`, IBGE `4208203` Itajaí)

2. ~~**Cliente INOVE**~~ — **FIXADO** `ie_indicator=9`, IE limpa (texto `ISENTO` + indicador `1` não bastava).

CT-e readiness gates = **PASS** (faltam só credenciais locais p/ `--live-cte`: `SUPABASE_SERVICE_ROLE_KEY` + `PW_TEST_*`).

## Blockers MDF-e

3. ~~OS sem driver/placa~~ — **OK** JOBERY + `KRE7A99`  
   (`driver_id=4159c985-…` · `vehicle_id=dcd905f6-…` · CPF ok)
4. ~~CT-e autorizado~~ — OK (reemit após cancel #9).
5. **Owner LINDACI sem RNTRC** — SEFAZ rejeita `rntrc_proprietario_veiculo=""`.  
   Fix mapper: omite grupo prop sem RNTRC 8 dígitos (trata frota própria Vectra).  
   Ideal: cadastrar RNTRC 8 dígitos no owner do veículo `KRE7A99`.
6. Quote sem `destination_uf` / `destination_ibge` / shipper IBGE → fallback client + VECTRA_IBGE_MUN.
7. (warn) sem CIOT — gap `infCIOT` no mapper (conhecido).

## Como rodar live (depois dos fixes)

```bash
# 1) .env.local: SUPABASE_SERVICE_ROLE_KEY + PW_TEST_USER/PASSWORD
# 2) secrets endereço VECTRA_* setados
# 3) ie_indicator=9 no cliente INOVE

npx tsx scripts/smoke-cte-mdfe-homolog.ts --os=OS-2026-08-0001 --live-cte

npx tsx scripts/smoke-cte-mdfe-homolog.ts --os=OS-2026-08-0001 --live-mdfe \
  --vehicle-id=<uuid> \
  --driver-id=1dbccd4e-558c-42be-8909-6a732d980f98
```

Live bate Focus/SEFAZ **homolog** e consome número de sequência.
