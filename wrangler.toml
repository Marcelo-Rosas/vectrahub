name = "vectrahub"
compatibility_date = "2025-03-07"
pages_build_output_dir = "dist"

# Prefer wrangler.jsonc (fonte canônica). Este TOML fica por compatibilidade
# com scripts/CI que ainda leem wrangler.toml.
# NUNCA coloque secrets aqui — use Cloudflare Pages → Settings → Environment Variables
# ou `wrangler pages secret put`.

[vars]
# VITE_* vêm dos secrets do GitHub Actions no build (não do runtime Pages).
